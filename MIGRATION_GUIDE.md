# คำแนะนำการอัปเดตระบบสถานะ Applications

## สิ่งที่เปลี่ยนแปลง

### 1. สถานะใหม่
- **pending** (รอพิจารณา) - ยังคงเหมือนเดิม
- **in_progress** (กำลังดำเนินงาน) - แทนที่ "approved"
- **completed** (เสร็จสิ้น) - สถานะใหม่
- **terminated** (เลิกจ้าง) - แทนที่ "rejected"

### 2. การแสดงผลในหน้าต่างๆ
- **หน้าผู้สมัครงาน** (`/shop-owner/applications`) - แสดงเฉพาะ `pending` และ `in_progress`
- **หน้าประวัติ** (`/shop-owner/history`) - แสดงเฉพาะ `completed` และ `terminated`

## ขั้นตอนการ Migration

### ถ้ามี Data อยู่ในฐานข้อมูล:

1. เปิด PostgreSQL client (pgAdmin, psql, หรือ DBeaver)

2. เชื่อมต่อกับฐานข้อมูลของคุณ

3. รันคำสั่ง SQL จากไฟล์ `prisma/migrations/update_application_status.sql`:

```sql
-- Convert existing status values
UPDATE applications 
SET status = CASE 
    WHEN status = 'approved' THEN 'in_progress'
    WHEN status = 'rejected' THEN 'terminated'
    ELSE status
END
WHERE status IN ('approved', 'rejected', 'pending');
```

4. ตรวจสอบผลลัพธ์:

```sql
SELECT status, COUNT(*) as count 
FROM applications 
GROUP BY status;
```

### ถ้ายังไม่มี Data:

ไม่ต้องทำอะไร โปรแกรมจะใช้สถานะใหม่โดยอัตโนมัติ

## หมายเหตุ

- ⚠️ ระบบจะไม่แสดงข้อมูลที่มีสถานะ `completed` และ `terminated` ในหน้า Applications อีกต่อไป
- ✅ ข้อมูลเหล่านั้นจะแสดงในหน้าประวัติแทน
- 🔄 Dropdown สำหรับเปลี่ยนสถานะจะมีตัวเลือกใหม่ 4 แบบ

## การทดสอบ

1. เข้าหน้า `/shop-owner/applications` ตรวจสอบว่ามีเฉพาะผู้สมัครที่กำลังดำเนินงานหรือรอพิจารณา
2. เปลี่ยนสถานะผู้สมัครเป็น "เสร็จสิ้น" หรือ "เลิกจ้าง"
3. ตรวจสอบว่าผู้สมัครหายไปจากหน้า Applications
4. เข้าหน้า `/shop-owner/history` เพื่อดูข้อมูลที่เสร็จสิ้นแล้ว
